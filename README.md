# tandem — агент тарифных кампаний

Beeline Tariff Marketing Campaigns Case.

Агент должен исследовать аудиторию пилотами и выбрать до 10 маркетинговых
кампаний, максимизируя ожидаемый прирост ARPU за вычетом стоимости контактов.
Все предоставленные данные синтетические: результаты не описывают реальный
бизнес Beeline.

## Текущее состояние

Текущая версия содержит исходные данные, среду оценки и инструмент
воспроизводимого измерения стартового агента.
Собственный `agent.py` находится в разработке и ещё не добавлен. Стартовый
шаблон имеет отрицательный результат во всех проверенных seed=0..9;
это зафиксированная точка отсчёта, а не готовая стратегия команды.

## Установка и запуск текущего baseline

Требуется Python 3.12. Проверено на CPython 3.12.13, pandas 3.0.6, numpy 2.5.3.
Команды выполняются из корня репозитория. GPU, API-ключи и платные аккаунты
для текущей проверки не нужны.

```bash
python -m venv .venv
# Windows PowerShell:
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe tools/run_baseline.py --runs 10
```

Linux/macOS после создания окружения:

```bash
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python tools/run_baseline.py --runs 10
```

Результат: `reports/baseline.json`, таблица результатов по seed и отдельный
`reports/baseline_submission.csv`, полученный от исходного шаблона при seed=42.
Проверяется совпадение двух генераций этой таблицы. Этот CSV не является
финальной сдачей команды.

## Архитектура

```text
customer_profile.csv + data/ + справочники
                 ↓
Agent.act(env): выбор пилотов → обновление оценок → план кампаний
                 ↓
публичный scoring_core.py → чистый прирост, расходы, охват
                 ↓
make_submission.py → воспроизводимая таблица финальных кампаний
```

`tools/run_baseline.py` импортирует неизменённый `agent_template.Agent` и
использует официальные `evaluate_agent` и `build_submission`.
Стратегия команды будет реализована отдельно в `agent.py` и `strategy/`.

## Данные и ограничения

- 23 441 уникальный абонент, 21 тариф, 14 823 строки истории переходов.
- Бюджет 100 000 у.е.; до 15 000 контактов вместе с пилотами.
- До 20 пилотов по 10–200 абонентов; 1–10 финальных кампаний;
  до 5 000 контактов на кампанию.
- Повторные контакты расходуют ресурсы; эффект человека учитывается один раз.
- Пилоты шумные, эффекты на судействе отличаются от локальной модели.
- В профиле есть пропуски, включая 95 отсутствующих текущих тарифов.
- `PASS` локального оценщика означает положительный net, но не заменяет
  проверку всех must-have.

## Источники и использование AI

Исходные файлы `agent_template.py`, `environment.py`, `mock_environment.py`,
`scoring_core.py`, `local_eval.py`, `make_submission.py`, CSV и
`PARTICIPANT_GUIDE.md` предоставлены организаторами и включены без изменений.
Они не являются собственной разработкой команды. Источник:
[официальный пакет](https://drive.google.com/file/d/1cQUKtE_cm9TVXgzpcFwQYYUpmuFaJHHT/view).
Условия исходных материалов не заменяются новой лицензией в этом репозитории.

Команда использует Codex для анализа, разработки и документации.
Авторы команды проверяют результаты AI и отвечают за решения.

## Проверка и экспорт кампаний

После реализации `agent.py`: `python local_eval.py`, затем
`python local_eval.py --runs 10`, затем `python make_submission.py`.
Последняя команда формирует `submission.csv` с выбранными кампаниями.
